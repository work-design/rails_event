<table class="table is-hoverable is-fullwidth">
  <thead>
    <tr>
      <th><%= Eventual::TimePlan.human_attribute_name(:id) %></th>
      <th><%= Eventual::TimePlan.human_attribute_name(:place_id) %></th>
      <th><%= Eventual::TimePlan.human_attribute_name(:repeat_type) %></th>
      <th><%= Eventual::TimePlan.human_attribute_name(:repeat_days) %></th>
      <th>
        <p><%= TimePlan.human_attribute_name(:next_start_time) %></p>
        <p><%= TimePlan.human_attribute_name(:next_finish_time) %></p>
      </th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
  <% @time_plans.each do |plan| %>
    <tr>
      <td><%= plan.id %></td>
      <td><%= plan.place_id %></td>
      <td><%= plan.repeat_type %></td>
      <td><%= plan.repeat_days %></td>
      <td>
        <p><time data-controller="time"><%= plan.next_start_time&.to_s(:rfc822) %></time></p>
        <p><time data-controller="time"><%= plan.next_finish_time&.to_s(:rfc822) %></time></p>
      </td>
      <td class="ui labels">
        <%= link_to time_plan_path(params[:plan_type], params[:plan_id], plan), method: :delete, aria: { label: t('.destroy.title') }, data: { turbo_confirm: t('.confirm') }, class: 'ui red mini icon button' do %>
          <i class="times icon"></i>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
