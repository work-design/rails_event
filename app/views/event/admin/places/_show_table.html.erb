<table class="table is-hoverable is-fullwidth">
  <thead>
    <tr>
      <th class="six wide">
        <%= link_to t('.edit'), edit_org_panel_place_path(@place), class: 'ui mini blue right floated button', remote: true %>
      </th>
      <th class="ten wide"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="has-text-right"><%= Place.human_attribute_name(:place_number) %></td>
      <td><%= @place.place_number %></td>
    </tr>
    <tr>
      <td class="has-text-right"><%= Place.human_attribute_name(:time_plans_count) %></td>
      <td><%= @place.time_plans_count %></td>
    </tr>
  </tbody>
</table>
