<table class="table is-fullwidth">
  <thead>
    <tr>
      <th class="six wide">
        <%= link_to t('.edit'), edit_admin_place_taxon_path(@place_taxon), class: 'ui mini blue right floated button', remote: true %>
      </th>
      <th class="ten wide"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="right aligned"><%= PlaceTaxon.human_attribute_name(:name) %></td>
      <td><%= @place_taxon.name %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= PlaceTaxon.human_attribute_name(:position) %></td>
      <td><%= @place_taxon.position %></td>
    </tr>
    <tr>
      <td class="right aligned"><%= PlaceTaxon.human_attribute_name(:places_count) %></td>
      <td><%= @place_taxon.places_count %></td>
    </tr>
  </tbody>
</table>
